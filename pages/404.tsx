import { NextPage } from 'next';

import { NotFoundPageContent } from '@pages-content/NotFoundPageContent';

const NotFoundPage: NextPage = () => {
  return <NotFoundPageContent />;
};

export default NotFoundPage;
